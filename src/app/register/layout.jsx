export const metadata = {
    title: "Register",
    description: "Driving School",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}